# Backend environment variables
# DATABASE_URL supports any SQLAlchemy URL, defaults to sqlite:///./data.db if not set.
# For SQLite file in backend directory:
# DATABASE_URL=sqlite:///./data.db
# For absolute path (example):
# DATABASE_URL=sqlite:////home/user/data/testcases.db

# DATABASE_URL=
